#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <stddef.h>
#include <stdlib.h>

int _printf(const char *format, ...);

/**
 * currently only found one prototype, add more if needed
 */

#endif
